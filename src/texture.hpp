#ifndef TEXTURE_H
#define TEXTURE_H

#include <GL/gl.h>
#include <GL/glut.h>
// #include <string>

// using namespace std;

GLuint LoadTexture(const char* filepath);
// GLuint LoadTexture(string filepath);

#endif